/**
 * Converts a given string to camelCase format.
 *
 * Handles various delimiters (spaces, hyphens, underscores), trims whitespace, and removes non-alphanumeric characters except for letters and digits.
 * Consecutive delimiters are treated as a single separator.
 * The first word is converted to lowercase, and subsequent words are capitalized.
 *
 * Edge Cases:
 * - If the input is an empty string or contains only delimiters/special characters, returns an empty string.
 * - If the input is not a string, throws a TypeError.
 *
 * @param {string} input - The string to convert to camelCase.
 * @returns {string} The camelCase formatted string.
 * @throws {TypeError} If the input is not a string.
 */

/**
 * Converts a given string to dot.case format.
 *
 * Handles various delimiters (spaces, hyphens, underscores), trims whitespace, and removes non-alphanumeric characters except for letters and digits.
 * Consecutive delimiters are treated as a single separator.
 * All words are converted to lowercase and joined by dots.
 *
 * Edge Cases:
 * - If the input is an empty string or contains only delimiters/special characters, returns an empty string.
 * - If the input is not a string, throws a TypeError.
 *
 * @param {string} input - The string to convert to dot.case.
 * @returns {string} The dot.case formatted string.
 * @throws {TypeError} If the input is not a string.
 */
